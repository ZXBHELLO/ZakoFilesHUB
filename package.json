{
  "name": "hf-image-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx wrangler pages publish dist --project-name=hf-image-hub",
    "deploy:dry": "vite build && npx wrangler pages publish dist --project-name=hf-image-hub --dry-run"
  },
  "dependencies": {
    "@huggingface/hub": "^2.15.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
